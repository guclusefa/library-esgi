{% extends 'base.html.twig' %}

{% block title %}Modifier le livre : {{ book.name }}{% endblock %}

{% block body %}
    <h1>Modifier le livre : {{ book.name }}</h1>
    <section class="py-3">
        {% include('frontend/book/_form.html.twig') with {'button_label': 'Modifier'} %}
    </section>

    <section class="py-3">
        <div class="row">
            <div class="col">
                <a class="btn btn-secondary" href="{{ path('app.books') }}">Retour à la liste</a>
            </div>
        </div>
    <section class="py-3">
{% endblock %}