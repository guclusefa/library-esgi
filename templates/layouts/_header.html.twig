<header class="shadow">
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app.home') }}">LIBRARY ESGI</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav gap-2">
{#                    <li class="nav-item">#}
{#                        <a class="nav-link" href="{{ path('app.books') }}">Livres</a>#}
{#                    </li>#}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app.authors') }}">Auteurs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app.categories') }}">Catégories</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>